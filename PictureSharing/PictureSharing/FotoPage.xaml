<Page
    x:Class="PictureSharing.FotoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PictureSharing"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.DataContext>
        <local:Foto/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <StackPanel>
            <StackPanel Padding="20" Orientation="Horizontal">
                <!-- Gebruik W10 back button 
                    <Button x:Name="btnBack" Content="Ga terug" Margin="0 0 0 0" Click="btnBack_Click" Style="{StaticResource NavigationBackButtonNormalStyle}"/>
                -->
                <TextBlock Margin="20, 0, 0, 0" Style="{ThemeResource TitleTextBlockStyle}" Text="{Binding fotoNaam}"/>
            </StackPanel>
            <Image x:Name="image" Height="auto" Width="auto" Source="{Binding path}" Stretch="Fill"/>
        </StackPanel>

    </Grid>
</Page>
